# Core decoding (bundles FFmpeg - no separate system install needed)
av>=11.0.0,<13.0.0

# Windows-compatible audio output (PortAudio bindings)
sounddevice>=0.4.6,<0.5.0

# Numeric sample buffers
numpy>=1.24.0,<3.0.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
